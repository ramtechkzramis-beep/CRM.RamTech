-- Новый вид переписки — групповой чат с ручным составом участников,
-- в отличие от чата отдела, где состав считается сам от department_id.
-- Отдельная миграция: новое значение enum нельзя использовать в той же
-- транзакции, где оно добавлено (тот же нюанс, что и с ролью developer).
alter type conversation_kind add value 'group';
